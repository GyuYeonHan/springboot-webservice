spring:
  profiles:
    active: local

  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    hikari:
      username: gyuyeon
      password: as998077df
      driver-class-name: org.mariadb.jdbc.Driver

#    url: jdbc:h2:tcp://localhost/~/subtogether
    url: jdbc:mariadb://springboot2-webservice.cwick4ljnkvk.ap-northeast-2.rds.amazonaws.com:3306/springboot2_webservice

  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL57Dialect
#          storage_engine: innodb

    hibernate:
      ddl-auto: create

  session:
    store-type: jdbc

logging.level:
  org.hibernate.SQL: debug
